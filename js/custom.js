
// jQuery code here
$(function () {
    // preloader start
    $(window).load(function () {
      $("#loading").fadeOut(400);
    });
    // preloader end
  });

  // aos animation
AOS.init({
    
});
